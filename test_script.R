#This is a test script to verify that DDA's BitBucket repository is working 

library(ggplot)

#Now that we have loaded the ggplot library, we can load the nycflights library

library(nycflights13)

a <- 1+1 